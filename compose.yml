services:
  jordi-codes:
    build: .
    restart: unless-stopped
    ports:
      - "22:22"
      - "3000:80"
    volumes:
      - ssh-host-key:/app/.ssh
    environment:
      TERM: xterm-256color
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}

volumes:
  ssh-host-key:
